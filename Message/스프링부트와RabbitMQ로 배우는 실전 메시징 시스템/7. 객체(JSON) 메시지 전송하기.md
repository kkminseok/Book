이전장에서는 간단한 String객체를 보냈지만, 클래스처럼 복잡한 구조의 데이터를 어떻게 보낼까에 대한 이야기다.

Spring AMQP가 기본적으로 사용하는 직렬화/역직렬화 컨버터는 `SimpleMessageConvert`인데, 이는 자바라는 언어에만 국한되게 직렬화/역직렬화 가능성이 높기 때문에
`Jackson2JsonMessageConverter`을 사용하는것을 권장한다는 것이 이번장에서 가장 가져갈만한 내용이다.

나머지는 애플리케이션에서 어떻게 주고받는지 예제이므로 따로 정리하지는 않겠다.